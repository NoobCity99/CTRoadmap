# CTRoadmap Project Log

This file tracks planning questions, product decisions, bugs, and bug fixes discussed during the CTRoadmap build.

## Questions And Answers

| Date | Question | Answer |
|---|---|---|
| 2026-06-28 | What should the first completed build milestone include? | MVP editor only: tiles, subtiles, relationships, save/load, Docker. Exports are deferred. |
| 2026-06-28 | Which GUI template should be default or included? | Ship both `canvas_topology` and `layered_hierarchy` templates. |
| 2026-06-28 | How should first-run sample data work? | Start blank by default and provide optional CTDC seed loading. |
| 2026-06-28 | What should Phase 2 exports include by default? | Full atlas exports, not active-view-only exports. |
| 2026-06-28 | How far should Phase 2 saved-view editing go? | Add create, edit, and delete saved views. |
| 2026-06-28 | How should imported atlas JSON behave? | Validate through the backend and replace the current atlas only after validation succeeds. |
| 2026-06-28 | How should Phase 3 expose flow-step editing? | Add ordered step editing inside the flow tile inspector. |
| 2026-06-28 | Should flow step endpoints be free text or tile references? | Flow step `from` and `to` values must reference existing atlas tiles. |
| 2026-06-28 | Should Phase 3 add HERMES-specific YAML? | Defer HERMES-specific YAML structure; preserve canonical flow/check data only. |
| 2026-06-29 | What should the first Settings release include? | Active Settings panel with UI palette selection, app metadata, backend health status, and exportable debug log. |
| 2026-06-29 | Should debug log exports include atlas contents? | No. Debug exports include event metadata and summary counts only, with secret-like context keys redacted. |

## Product Decisions

| Date | Decision | Reason |
|---|---|---|
| 2026-06-28 | `data/atlas.json` is the canonical source of truth. | Keeps the app local-first, readable, and backup-friendly. |
| 2026-06-28 | The MVP does not execute shell, SSH, Docker, or validation commands. | Preserves the atlas/documentation scope and avoids unsafe automation paths. |
| 2026-06-28 | Export controls are scaffolded but disabled in the MVP UI. | Keeps the user-facing layout aligned with the planned product without implementing Phase 2 behavior early. |
| 2026-06-28 | Tile creation should live in the left Tile Palette, not the top toolbar. | The top `New Tile` button and tile-type dropdown duplicated the palette; palette buttons now create tiles by click or drag/drop. |
| 2026-06-28 | Phase 2 exports should generate Markdown, YAML, and Mermaid files from the full canonical atlas. | Keeps documentation output complete and avoids adding per-view export complexity before it is needed. |
| 2026-06-28 | Saved view filters should be stored in `atlas.views`. | Makes view behavior persistent, backup-friendly, and available to exports/imports. |
| 2026-06-28 | Flow/check features remain non-executing in Phase 3. | The app is still an atlas and planning tool, not an automation or monitoring runner. |
| 2026-06-29 | Settings are browser-local UI preferences, not atlas data. | Palette and collapse state should not modify `data/atlas.json` or require schema migration. |
| 2026-06-29 | Parent collapse/expand state is local UI state for this pass. | It improves canvas navigation without changing canonical topology data. |
| 2026-06-29 | Search results should be actionable from the left panel. | Users need direct selection/focus of matching tiles and relationships, including items hidden under collapsed parents. |
| 2026-06-29 | Hierarchy and communication should use separate visual linkage paths. | Parent/child containment remains bottom-to-top, while calls/flows/dependencies use right-side `OUT` to left-side `IN` handles. |
| 2026-06-29 | Collapse/expand controls should be removed from the canvas. | Testing showed hiding descendants is not necessary, and always-visible hierarchy avoids stale local collapse state hiding tiles. |
| 2026-06-29 | Planning work should persist as atlas data. | Planned objects are stored as `lifecycle: planned` on normal tiles and links instead of a temporary overlay or separate file. |
| 2026-06-29 | Live View remains editable for live objects. | Planning Mode edits only planned objects, while Live View can still maintain current live atlas data and promote planned work. |
| 2026-06-28 | Beta users should install from a published Docker image instead of cloning the repo. | The first release target is a Docker-image-based install using `ghcr.io/noobcity99/ctroadmap:beta`. |
| 2026-06-29 | Optional sample seed data should be stored as atlas JSON instead of hardcoded TypeScript. | Keeps the seed file aligned with exported atlas data and makes future seed replacement straightforward. |
| 2026-07-01 | Update Advisory must remain informational only. | CTRoadmap can check a public version manifest and show update guidance, but it must not auto-update, run Docker commands, mount the Docker socket, or execute system-management actions. |

## Release Changes

| Date/Time | Files Created/Changed | Summary | Assumptions | Validation Results |
|---|---|---|---|---|
| 2026-07-11 | Created `frontend/src/lib/icons.tsx`; changed `frontend/src/lib/constants.ts`, `frontend/src/components/Inspector.tsx`, `frontend/src/components/TileNode.tsx`, `frontend/src/components/HandbookView.tsx`, and `PROJECT_LOG.md`. | Refactored icon-library resolver/rendering code out of static app constants and into a dedicated icon module while preserving uploaded/Lucide icon behavior, edit/delete mode, and tile/Handbook/canvas rendering. | `.gitignore` now permits new files under `frontend/src`; this is a behavior-preserving frontend-only cleanup, so backend routes and models were not changed. | `npm --prefix frontend run build` passed with the existing Vite chunk-size warning; backend compile was not rerun because no backend files were changed for this refactor. |
| 2026-07-11 | Changed `frontend/src/App.tsx`, `frontend/src/components/CanvasFrame.tsx`, `frontend/src/components/AvoidTilesEdge.tsx`, `frontend/src/lib/graphMapping.ts`, `frontend/src/lib/edgeRouting.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added a browser-local connector routing toggle on the canvas with Curved and Avoid Tiles modes; Avoid Tiles uses custom orthogonal edge rendering around visible tile boxes and Family headers while preserving link handles and atlas link data. | Routing mode is stored only in `localStorage`; atlas JSON, backend schemas, imports, exports, and `atlas.links` are unchanged. Curved remains the default and fallback rendering mode. | `npm --prefix frontend run build` passed with the existing Vite chunk-size warning. |
| 2026-07-11 | Changed `backend/app/main.py`, `frontend/src/components/Inspector.tsx`, `frontend/src/lib/api.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added uploaded-icon library edit mode in the Tile Inspector: `Edit` toggles red remove buttons on uploaded icons, `Done` exits edit mode, uploaded assets can be deleted from `data/assets/icons`, the upload button aligns icon/text per the pane design, and `Use Default Icon` was shortened to `Use Default`. | Deleting an uploaded asset removes it from the shared icon library and clears the current tile only if that deleted asset is currently selected; unrelated tile fields and other tile refs are not rewritten. | `npm --prefix frontend run build` passed with the existing Vite chunk-size warning; `python3 -m compileall backend` passed. |
| 2026-07-11 | Changed `backend/app/main.py`, `frontend/src/components/Inspector.tsx`, `frontend/src/components/TileNode.tsx`, `frontend/src/components/HandbookView.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/constants.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Replaced the Tile Inspector upload-only icon control with an inline expandable Icon Library, added uploaded-icon reuse from `data/assets/icons`, added curated Lucide icon selection, and centralized icon rendering for Inspector, canvas tiles, and Handbook. | Existing uploaded icon refs remain valid; new resolver IDs use `uploaded:<filename>` and `lucide:<IconName>`; SVG uploads and full Lucide catalog import remain excluded; exports are not redesigned. | `npm --prefix frontend run build` passed with the existing Vite chunk-size warning; `python3 -m compileall backend` passed. |
| 2026-07-09 | Changed `latest.json`, `Dockerfile`, `backend/app/update_advisory.py`, `backend/app/main.py`, `frontend/package.json`, `frontend/package-lock.json`, and `PROJECT_LOG.md`. | Updated release metadata to `0.2.0-beta`, including the public update manifest so existing beta installs can see the update advisory. | The published beta image/build should use the same `CTR_VERSION=0.2.0-beta`; `latest.json` remains advisory-only and does not auto-update installations. | JSON metadata parse passed; `npm --prefix frontend run build` passed; `docker compose build` passed; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; container version check printed `0.2.0-beta`. |
| 2026-07-08 | Changed `frontend/src/App.tsx`, `frontend/src/components/HandbookView.tsx`, `frontend/src/components/LeftSidebar.tsx`, `frontend/src/lib/handbook.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Compact Handbook tile relationships by grouping raw link types, added a persistent local Handbook Dark/Light display toggle, and replaced Outline active-section tracking so the final tile can become active at the bottom of a Volume or when selected. | Handbook display mode is local browser UI state only; raw relationship type grouping is used for both incoming and outgoing links; no backend, atlas schema, autosave, import/export, or topology-lock changes were needed. | `npm --prefix frontend run build` passed. |
| 2026-07-08 | Changed `frontend/src/App.tsx`, `frontend/src/components/HandbookToc.tsx`, `frontend/src/components/HandbookView.tsx`, `frontend/src/components/LeftSidebar.tsx`, `frontend/src/lib/handbook.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Updated Handbook presentation so the document renders one selected Volume at a time, the ToC title reads `TABLE OF CONTENTS`, the active Volume is highlighted with an amber neon outline, Canvas-to-Handbook switching opens the selected tile's Volume when possible, and the Outline shows only the active Volume with every tile tiered by depth. | This is a frontend presentation/state update only; Family normalization, autosave, save/load/import/export behavior, and Handbook topology locks remain unchanged. | `npm --prefix frontend run build` passed. |
| 2026-07-08 | Changed `backend/app/models.py`, `frontend/src/App.tsx`, `frontend/src/components/Inspector.tsx`, `frontend/src/components/LeftSidebar.tsx`, `frontend/src/lib/atlasSelectors.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`; created `frontend/src/lib/handbook.ts`, `frontend/src/components/HandbookView.tsx`, and `frontend/src/components/HandbookToc.tsx`. | Reimplemented Handbook as the user-facing replacement for the disabled hierarchy template, with full-atlas volumes/chapters/sections, Handbook ToC, sticky outline, on-page notes editing, URL/path field interactions, and Handbook-mode topology locks. Backend and frontend Family normalization now enforce one-Family membership plus ancestor/descendant inheritance. | Legacy `layered_hierarchy` remains accepted and maps to Handbook in the UI; Markdown/YAML/Mermaid exports are preserved rather than reorganized; Handbook notes remain Markdown-compatible plain text in `tile.notes`; invalid imported Family conflicts are resolved by ascending `family.order`, with existing order as tie-breaker. | `npm --prefix frontend run build` passed; local `python3 -m compileall backend` passed; local Pydantic snippet could not run because host Python lacks `pydantic`; `docker compose build` passed after Docker/buildx escalation; `docker compose up -d` passed after Docker socket escalation; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; container Pydantic snippets for Family normalization passed. |
| 2026-07-07 | Changed `frontend/src/lib/api.ts`, `TECHNICAL_ARCHITECTURE.md`, and `PROJECT_LOG.md`. | Centralized frontend API request discipline with shared JSON/FormData/void request helpers and typed `ApiError` messages that prefer FastAPI `detail`; documented the API boundary and post-App-refactor frontend layout. | Behavior-preserving frontend refactor only; exported API function names, backend endpoints, atlas schema, autosave, imports, exports, and downloads remain unchanged. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed for `frontend/src/lib/api.ts` and `TECHNICAL_ARCHITECTURE.md`. `PROJECT_LOG.md` still reports pre-existing CRLF/trailing-whitespace noise when included in diff checks. |
| 2026-07-06 | Changed `frontend/src/App.tsx`; created `frontend/src/components/TopBar.tsx`, `LeftSidebar.tsx`, `LayerBar.tsx`, `CanvasFrame.tsx`, `ExportMenu.tsx`, `SearchBox.tsx`, `frontend/src/lib/atlasSelectors.ts`, `atlasMutations.ts`, and `graphMapping.ts`; changed `PROJECT_LOG.md`. | Started the App orchestration refactor by extracting pure atlas helpers, React Flow graph mapping, and prop-driven presentational UI regions while keeping atlas mutation callbacks in `App.tsx`. | Behavior-preserving frontend refactor only; no backend, atlas schema, API, CSS redesign, or autosave semantic changes were intended. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed for changed frontend source files. `PROJECT_LOG.md` still reports pre-existing CRLF/trailing-whitespace noise when included in diff checks. |
| 2026-06-28 23:56 PDT | Created `CTRBETA_release-compose.yml`, `CTR_install.sh`, `CTR_uninstall.sh`; changed `README.md` and `PROJECT_LOG.md`. | Docker-image-based beta release bootstrap added. | Image will be published as `ghcr.io/noobcity99/ctroadmap:beta`; installer targets Linux with Docker Compose v2. | `bash -n CTR_install.sh` passed; `bash -n CTR_uninstall.sh` passed; `docker compose -f CTRBETA_release-compose.yml config` passed; `git diff --check` passed. |
| 2026-07-01 | Created `backend/app/update_advisory.py` and `latest.json`; changed Dockerfile, backend API, frontend API/types/settings/UI, README, `.gitignore`, and project log. | Advisory-only update checks added with persisted `data/update_state.json` state and Settings/toolbar guidance. | Default deployment is Docker beta; default manifest is GitHub raw `main/latest.json`; users manually run the copied update command. | `python3 -m compileall backend` passed; `npm --prefix frontend run build` passed; `python3 -m json.tool latest.json` passed; targeted `git diff --check` passed for source changes. Repo-wide whitespace check still reports unrelated CRLF/trailing-whitespace issues in modified docs. |
| 2026-07-01 | Changed `frontend/src/App.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added local collapsible left-sidebar sections for Tile Palette, Views, Filters, and Relationships; collapsed Tile Palette shows a three-item infinite cycling picker with only the centered tile interactive. | Sidebar collapse state remains browser-local in `localStorage` and does not change atlas JSON or backend APIs. | `npm --prefix frontend run build` passed. Backend runtime checks were not run because this was a frontend-only change. |
| 2026-07-02 | Changed `frontend/src/components/Inspector.tsx`, `frontend/src/components/TileNode.tsx`, `frontend/src/lib/constants.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added a node-only Primary Node toggle that enlarges primary node tiles, thickens their border, and brightens their glow. | Primary node state is stored as `fields.primary_node` on node tiles; no backend API or atlas schema change was required. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed backend atlas/export models plus frontend atlas rendering, stack inspector, tile styling, and project log. | Added persistent visual stacks for same-parent/same-type child tiles using optional `atlas.stacks`; stacked members stay in atlas data while hidden members visually reroute links to the representative. | Stacks are visual state, not infrastructure objects; hidden-member search selects the visible stack representative. | `npm --prefix frontend run build` passed; `docker compose build` passed; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed stack models, stack rendering, stack context menu, stack inspector, stack export text, CSS, and project log. | Extended persistent visual stacks to Mount parent tiles so mixed-type child tiles can be hidden under the Mount representative with a hex count badge and `X Mounted Items` readout. | Mount-child stacks use `stack_kind: mount_children`; the Mount tile is the representative and member count excludes the Mount itself. | `npm --prefix frontend run build` passed; `docker compose build` passed; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/components/TileNode.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added larger Primary Node title text, clickable URL field previews, and click-to-copy Path field previews with a temporary confirmation toast. | URL/path interactions are frontend-only display behavior and do not change atlas data or backend APIs. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed backend asset config/storage/API, backend requirements, frontend API/types, tile inspector/rendering, CSS, and project log. | Added per-tile custom uploaded icon overrides stored under `data/assets/icons` with references in `fields.icon_ref`; default tile-type icons remain the fallback. | SVG uploads are excluded for now; PNG, JPEG, and WebP are accepted up to 512 KB. Icon files must be backed up with `data/` for full portability. | `npm --prefix frontend run build` passed; `docker compose build` passed; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/components/SettingsPanel.tsx`, `frontend/src/lib/theme.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added a browser-local Canvas Background setting with Grid and Hex options; Hex uses CSS-only layered gradients and preserves theme palette colors. | Canvas background selection is stored in `localStorage` and does not change atlas JSON or backend APIs. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/lib/theme.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added the user-contributed Circuit canvas background as a third Settings option using self-contained CSS layers. | Circuit excludes the existing app radial glow and canvas base so the submitted design controls its own base, middle, and top layers. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx` and `PROJECT_LOG.md`. | Applied the React Flow viewport-attached dot background overlay to every canvas background option, including Hex and Circuit. | The dot overlay remains a viewport plane effect separate from each static CSS background pattern. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/lib/theme.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added the Blueprint palette and matching Blueprint background; selecting the Blueprint palette also switches the canvas background once while leaving the background dropdown independent afterward. | Blueprint is frontend-only local UI state; tile backgrounds use translucent white surfaces with dark blueprint text, and custom uploaded icon images are not recolored. | First `npm --prefix frontend run build` hit the 120s command timeout during Vite transform; rerun with a longer timeout passed. Targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/components/TileNode.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Updated Blueprint so canvas tile icons and Tile Palette entries use Cyber palette colors while Blueprint tile surfaces stay pale; darkened Blueprint map tile drop shadows by about 25%. | Uploaded custom icon images still render as provided; the Cyber color treatment applies to default lucide tile icons and palette item accents. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/styles.css` and `PROJECT_LOG.md`. | Fixed Blueprint palette with Grid or Hex background so those backgrounds reset to the neutral dark canvas instead of inheriting Blueprint blue. | Circuit and Blueprint backgrounds remain self-contained; Aurora/Ember Grid and Hex palette tinting is unchanged. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added debounced autosave for atlas mutations while preserving manual Save, import/export behavior, and drag-stop-only position persistence. | Autosave remains frontend-only and uses the existing `PUT /api/atlas`; browser-local UI preferences are not written to atlas JSON. | `npm --prefix frontend run build` passed; `docker compose build` passed after sandbox escalation for Docker buildx metadata; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Compacted the topbar by making Save icon-only, shortening atlas import/download/demo labels, and consolidating Markdown/YAML/Mermaid into a `3rd Party Export` popover. | Export behavior is unchanged and remains frontend-driven through the existing export handler; this was frontend-only. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Reworked the topbar into protected main/action/right zones so Search and Settings stay visible while action labels shrink or truncate. | Search and Settings have priority over full toolbar labels; this was frontend-only. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/lib/theme.ts`, `frontend/src/types/atlas.ts`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Updated Hex to use a conic-gradient honeycomb pattern and added a PCB Board canvas background with green base, gold grid, and static gold dot pattern. | PCB Board is a self-contained canvas background; React Flow's moving dot overlay remains active. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/styles.css` and `PROJECT_LOG.md`. | Fixed the `3rd Party Export` popover so the topbar action row no longer clips the Markdown/YAML/Mermaid menu and the topbar stacks above the canvas. | The Planning Mode passive-listener console message is treated as a non-blocking browser performance warning unless users report visible interaction lag. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-02 | Changed `frontend/src/App.tsx`, `frontend/src/components/TileNode.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Added outside-click closing for the `3rd Party Export` menu, made the Settings gear toggle the panel, removed live tile badges, updated the Load Demo warning, and made the canvas view bar collapsible to an Eye icon. | View bar collapse is local UI state only; planned lifecycle badges remain visible. | `npm --prefix frontend run build` passed; targeted `git diff --check` passed. |
| 2026-07-05 | Changed backend atlas/export models plus frontend atlas types, canvas rendering, Family inspector, tile membership UI, CSS, and project log. | Added persistent lightweight Families as semantic canvas grouping rectangles in `canvas_topology` with create/select/move/resize/rename/delete and tile membership controls. | Families are visual/semantic atlas objects only; they do not change tile parents, links, stacks, lifecycle, commands, storage, or layered hierarchy layout. | `npm --prefix frontend run build` passed; `docker compose build` passed after sandbox escalation for Docker buildx metadata; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; current atlas schema load printed `families 0`; targeted `git diff --check` still reports existing CRLF/trailing-whitespace noise in already-modified files. |
| 2026-07-05 | Changed `frontend/src/App.tsx`, `frontend/src/components/FamilyNode.tsx`, `frontend/src/components/Inspector.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Revised Family UI so Families can be created from the Tile Palette with `Familycon.png`, collapsed palette includes Family, Family rectangles use a larger angled header, only the header is interactive, header clicks zoom to Family bounds, and the Family Inspector has a swatch-based color selector. | Family remains a separate semantic grouping object, not a tile type; Family body clicks pass through to the canvas and movement starts from the header. | `npm --prefix frontend run build` passed; targeted `git diff --check` still reports existing CRLF/trailing-whitespace noise in already-modified files. |
| 2026-07-05 | Changed `frontend/src/App.tsx`, `frontend/src/components/FamilyNode.tsx`, `frontend/src/styles.css`, and `PROJECT_LOG.md`. | Fixed Family palette/icon and canvas interaction polish: Family now uses lucide `CircuitBoard`, dragging is constrained to the Family header, the angled header leans the correct direction, and Family resizing previews live during drag before committing on release. | Family body remains intended to behave like canvas space; no backend, schema, export, or tile behavior changes were made. | `npm --prefix frontend run build` passed; targeted `git diff --check` still reports existing CRLF/trailing-whitespace noise in already-modified files. |
| 2026-07-06 | Changed backend/frontend tile type definitions, tile palette constants, theme colors, seed view filters, Tile Inspector fields UI, tile creation naming, canvas layering, and project log. | Added normal `iot_device` / `IOT Device` tiles with lucide Router icon, added field removal, replaced new-tile title prompts with sequential `NEW <TYPE> X` titles, and layered links above Family rectangles while keeping normal tiles above links. | Existing saved views are not migrated; IoT appears in new/demo Physical and Services views. Family/link data remains canonical and unchanged. | `npm --prefix frontend run build` passed; `docker compose build` passed after sandbox escalation for Docker buildx metadata; `docker compose up -d` passed after Docker socket escalation; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` still reports existing CRLF/trailing-whitespace noise in already-modified files. |
| 2026-07-06 | Changed `frontend/src/App.tsx`, `frontend/src/components/SettingsPanel.tsx`, `frontend/src/lib/seed_atlas.json`, `backend/app/models.py`, `backend/app/main.py`, `backend/app/exports.py`, `README.md`, and `PROJECT_LOG.md`. | Rebranded user-facing saved Views as Layers and replaced new/demo defaults with Hardware, Software, Infrastructure, and Everything layer presets. | The persisted schema and compatibility boundary remain `atlas.views`; existing saved views are not renamed or migrated. | First `npm --prefix frontend run build` hit the 120s Vite transform timeout; rerun with longer timeout passed. `docker compose build` passed after sandbox escalation for Docker buildx metadata; `docker compose up -d` passed; `docker compose exec ctroadmap python -c "import fastapi, pydantic; print('backend deps ok')"` passed; `docker compose exec ctroadmap python -m compileall backend` passed; targeted `git diff --check` still reports existing CRLF/trailing-whitespace noise in already-modified files. |

## Bugs And Fixes

| Date | Bug | Fix |
|---|---|---|
| 2026-07-11 | Avoid Tiles connector routing could leave some stack/top-tile connections curved, place labels away from the visible line, and render the routing toggle lower than the React Flow lock control. | Added a best-effort orthogonal fallback for hard-to-route connectors, switched custom connector labels to SVG `EdgeText` so labels share the same coordinate space as paths, and restored the routing toggle's bottom panel margin while matching the lock button size. |
| 2026-06-29 | Layered template node clicks could pan to a blank area because focus used saved canvas coordinates instead of the rendered layered position. | Tile focus now centers on the current rendered React Flow node position with saved position as a fallback. |
| 2026-06-29 | Flow tile step action previews overflowed the tile when a flow had more than two steps. | Changed the flow tile `steps` preview to display only the numeric step count. |
| 2026-06-29 | Flow tiles displayed `[object Object]` for step data on the canvas because node field previews stringified the `steps` object array directly. | Added tile field preview formatting that renders flow step `action` text and uses safe summaries for other object or array fields. |
| 2026-06-29 | Dragging an existing tile could blank the canvas until refresh because React Flow controlled node updates were being written through canonical atlas state on every drag tick, and the debug export did not capture drag/error context. | Moved drag movement into local React Flow node state, commit final positions to the atlas on drag stop, and added drag/anomaly/error debug events. |
| 2026-06-28 | Docker frontend build failed because `LinkType.replaceAll` required a newer string lib and `NodeChange.id` was read before narrowing the React Flow union. | Replaced `replaceAll` with regex `replace` and added a typed position-change guard before reading `id` or `position`. |
| 2026-06-28 | Local backend dependency install failed under Python 3.14 because pinned `pydantic-core` does not support that interpreter yet. | Kept Docker on Python 3.12 and documented Python 3.12/3.13 for local backend development. |
| 2026-06-28 | Local Node/NPM validation is blocked because this WSL environment reports WSL 1 unsupported for Node. | Frontend files were statically reviewed; build should be run in Docker or a supported Node environment. |
| 2026-06-29 | Settings gear showed disabled cursor and `Settings planned` hover text. | Replaced it with an active Settings panel and removed the disabled button behavior. |
| 2026-06-29 | Search only filtered the canvas and did not show left-panel result targets. | Added a search results list with clickable tile/link results and focus behavior. |
| 2026-06-29 | Parent tiles did not support hiding descendants on dense maps. | Added local collapse/expand state and parent node controls that hide descendant tiles and related hidden edges. |
| 2026-06-29 | Non-hierarchy links visually reused the same top/bottom handle path as parent/child links. | Added optional link port metadata plus IN/OUT handles so relationship routing persists across save/load. |
| 2026-06-29 | Top-bar search required backspacing to clear a query. | Added an inline clear button that resets search and keeps focus in the search input. |
| 2026-06-29 | Persistent IN/OUT port labels obstructed tile content. | Changed port labels to appear only when hovering or focusing the matching connection handle. |
| 2026-06-29 | Fit View framed the atlas too tightly. | Added shared padded fit-view options for initial fit and the Controls fit button. |
| 2026-06-29 | Double-clicking blank canvas zoomed into the clicked area instead of fitting the full view. | Disabled React Flow double-click zoom and mapped blank-canvas double-click to padded Fit View. |
| 2026-06-29 | Interactivity lock prevented link creation but still allowed tile movement. | Added app-owned interactivity state that locks both node dragging and link creation while preserving selection and inspector editing. |
| 2026-06-29 | CTRoadmap needed a way to model planned infrastructure separately from current live infrastructure. | Added Planning Mode with lifecycle-aware styling, locking, editing, and Go Live promotion for planned tiles and relationships. |
| 2026-06-29 | Beta LAN deployment crashed in Chrome with `TypeError: crypto.randomUUID is not a function` because plain `http://SERVER-IP:8088` is not a secure browser context. | Replaced the unguarded frontend debug-event UUID call with a feature-detected ID helper that falls back to `crypto.getRandomValues()` or a timestamp/counter ID. |
| 2026-07-02 | The `3rd Party Export` button appeared unresponsive because the topbar action row clipped the absolutely positioned export popover, then the canvas painted over most of the visible menu. | Allowed the topbar action row to render overflow and gave the topbar a higher stacking layer, so the export menu is visible without changing export behavior. |
| 2026-07-02 | Several small UI behaviors needed cleanup: export menus persisted after outside clicks, Settings only opened from the gear, live tile badges cluttered tiles, Load Demo warning copy was too mild, and the view bar consumed canvas space. | Added outside-click menu closing, gear toggle behavior, planned-only tile badges, stronger Load Demo warning copy, and an Eye-controlled collapsible view bar. |
