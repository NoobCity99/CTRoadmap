services:
  ctroadmap:
    build: .
    container_name: ctroadmap
    ports:
      - "127.0.0.1:8088:8088"
    volumes:
      - ./data:/app/data
      - ./exports:/app/exports
    restart: unless-stopped
